<div class="form-container mb-5">
	<%= simple_form_for([:hr, @user]) do |f| %>

		<div class="form-group">
  		<%= f.input :first_name %>
	 	</div>

		<div class="form-group">
			<%= f.input :last_name %>
		</div>

		<div class="form-group">
			<%= f.input :email %>
		</div>

  	<div class="form-group">
  		<%= f.label :role %>
			<%= f.select :role, User.roles.keys.map { |role| [role.humanize, role] }, {}, class: 'js-selectize' %>
  	</div>

    <div class="form-group">
      <%= f.label :manager_id %>
      <%= f.collection_select :manager_id, User.all_except(current_user), :id, :full_name, { prompt: true }, { class: 'js-selectize' } %>
    </div>

    <div class="form-group">
      <%= f.label :counsellor_id %>
      <%= f.collection_select :counsellor_id, User.all_except(current_user), :id, :full_name, { prompt: true }, { class: 'js-selectize' } %>
    </div>

		 <div class="form-group">
    		<%= f.label :admin %>
    		<%= f.check_box :admin %>
  		</div>

  		 <div class="form-group">
    		<%= f.label :join_date %>
    		<%= f.date_field :join_date %>
  		</div>

		<%= f.submit 'Update User', class: 'button' %>
		<%= link_to "Cancel", hr_user_path(@user), class: 'button button--danger' %>
	<% end %>
</div>
